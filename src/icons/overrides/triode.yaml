# yaml-language-server: $schema=../../schemas/internal/gen-overrides.schema.json

global:
  image_scale: 1.1
label:
  tray-menu:
    from_image: triode.original.png
    output_shape: square
    image_scale: 1.0
