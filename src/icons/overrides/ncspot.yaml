# yaml-language-server: $schema=../../schemas/internal/gen-overrides.schema.json

label:
  discord-small-image:
    from_image: ncspot.transparent.png
    image_scale: 1.35
  tray-menu:
    image_mask: square
    output_shape: square
