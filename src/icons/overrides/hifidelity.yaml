# yaml-language-server: $schema=../../schemas/internal/gen-overrides.schema.json

label:
  tray-menu:
    from_image: hifidelity.cropped.png
    image_mask: circle
  logo-128:
    from_image: hifidelity.cropped.png
    output_shape: circle
