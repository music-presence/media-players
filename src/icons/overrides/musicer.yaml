# yaml-language-server: $schema=../../schemas/internal/gen-overrides.schema.json

global:
  image_mask: circle
label:
  tray-menu:
    from_image: musicer.alt.png
    image_mask: square
    output_shape: square
