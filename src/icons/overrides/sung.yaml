# yaml-language-server: $schema=../../schemas/internal/gen-overrides.schema.json

label:
  discord-small-image:
    from_image: sung.rounded.png
    image_mask: circle
    background: transparent
    image_scale: 1.19
