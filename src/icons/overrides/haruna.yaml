# yaml-language-server: $schema=../../schemas/internal/gen-overrides.schema.json

label:
  discord-small-image:
    from_image: haruna.padded.png
    image_mask: circle
    background: transparent
    image_scale: 1.19
  tray-menu:
    output_shape: square
