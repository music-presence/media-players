# yaml-language-server: $schema=../../schemas/internal/gen-overrides.schema.json

global:
  image_mask: circle
label:
  tray-menu:
    image_mask: square
    output_shape: square
  discord-small-image:
    image_mask: circle
    background: transparent
    image_scale: 1.125
