# yaml-language-server: $schema=../../schemas/internal/gen-overrides.schema.json

label:
  tray-menu:
    output_shape: square
  discord-small-image:
    image_mask: circle
    background: transparent
    image_scale: 1.19
