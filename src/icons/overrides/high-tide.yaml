# yaml-language-server: $schema=../../schemas/internal/gen-overrides.schema.json

global:
  image_scale: 1.05
label:
  discord-small-image:
    image_mask: circle
    background: transparent
    image_scale: 1.19
